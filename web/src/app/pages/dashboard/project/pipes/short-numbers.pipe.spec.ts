import { ShortNumbersPipe } from './short-numbers.pipe';

describe('ShortNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
